export type WorkType = {
    id: string;
    title: string;
    platform: string;
    usedService: string[];
    description: string;
    githubUrl?: string;
	date: string;
	team: string;
	role: string;
	serviceUrl?: string;
	imageCount: number;
};