export type WorkType = {
    id: string;
    imageSrc: string;
    title: string;
    platform: string;
    usedService: string[];
    description: string;
    githubUrl?: string;
	date: string;
	team: string;
	role: string;
	serviceUrl?: string;
};